---
import BackButton from "@/components/BackButton.astro";
import WipBanner from "@/components/WipBanner.astro";
import Layout from "@/layouts/Layout.astro";

interface Props {
  title: string;
  subtitle: string;
  status?: "wip" | "live";
  image?: string;
}

const { title, subtitle, status = "wip", image } = Astro.props;
---

<Layout {title} {subtitle} {image}>
  <BackButton />
  {status === "wip" && <WipBanner />}
  <slot />
</Layout>
