---
import { Font } from "astro:assets";
---

<Font cssVariable="--font-roboto" preload />
<Font cssVariable="--font-fira-mono" preload />

<style is:global>
  body {
    font-family: var(--font-roboto);
  }

  code, .monospace {
    font-family: var(--font-fira-mono);
  }
</style>
